{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<main>
		{% if is_granted('ROLE_ADMIN') %}
			<div class='content'>
				<div class="centered">
					<h1>Utilisateur {{ user.username }}</h1>
				</div>
                <div class="centered">

                <p>{{ user.id }}</p>

                <p>{{ user.email }}</p>

                <p>{{ user.roles ? user.roles|json_encode : '' }}</p>

                <p>{{ user.password }}</p>

                <p>{{ user.dateOfCreation ? user.dateOfCreation|date('Y-m-d') : '' }}</p>

                <p>{{ user.username }}</p>

                <p>{{ user.firstName }}</p>

                <p>{{ user.lastName }}</p>

                <p>{{ user.discountStatus }}</p>
                </div>
            </div>

    <a href="{{ path('user_index') }}">Retour à la liste</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">modifier</a>

    {{ include('user/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}

{% block stylesheets %}
	{{ encore_entry_link_tags('menu')  }}
{% endblock %}

{% block javascripts %}
	{{ encore_entry_script_tags('menu') }}
{% endblock %}

<body>


	<header>


		<div class="container">
			<div class="flex">
				<a href="{{ path("accueil") }}"><img class="logo" src="/img/headerFooter/logo.png" alt="logo"></a>
				<h1><a href="{{ path("accueil") }}">Shikeygami</a></h1>
			</div>

			<div class="burger-div">
				<img class="burger" src="/img/headerFooter/burger.png" alt="burger">
			</div>
		</div>

		<nav>
			<ul class="menu">
				<li>
					<a href="{{ path("game_index") }}">Nos salles</a>
				</li>

				<li>
					<a href="{{ path("concept") }}">Concept</a>
				</li>

				<li>
					<a href="{{ path("user_index") }}">EG virtuel</a>
				</li>

				{% if app.user %}
					<li>
						<a href="{{ path("user_index") }}">
							{{app.user.firstname}}</a>
					</li>

					<li>
						<a href="{{ path("app_logout") }}">Déconnexion</a>
					</li>

				{% else %}
					<li>
						<a href="{{ path("app_login") }}">Connexion</a>
					</li>

					<li>
						<a href={# "{{ path("app_register") }}" #}>Inscription</a>
					</li>
				{% endif %}
			</ul>
		</nav>
	</header>
</body>

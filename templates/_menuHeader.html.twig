<header>

<div class="wrapperCookie">
	<img class="imgCookie" src="/img/headerFooter/cookie-5784367_960_720.jpg" alt="cookie">
	<div class="contenu">

		<h5 class="titleCookie">Accepter les cookies</h5>
		<p class="textCookie">Ce site web utilise des cookies Gare à vous! Nous allons collecter vos données</p>
		<div class="buttonCookie">
			<button class="accept">Compris</button>
			<a class="infoCookie" href="{{ path("mentions_legales") }}">En savoir plus</a>
		</div>

	</div>
</div>

	<div class="container">
		<div class="flex">
			<a href="{{ path("accueil") }}"><img class="logo" src="/img/headerFooter/logo.jpg" alt="logo"></a>
			<h1 class="title-header">
				<a href="{{ path("accueil") }}" >Shikeygami</a>
			</h1>
		</div>

			<div class="burger-div">
				<img class="burger" src="/img/headerFooter/burger.jpg" alt="burger">
			</div>
		</div>
	</div>

	<div style="nav-content">
		<nav>
			<ul class="menu">
				<li>
					<a href="{{ path("game_index") }}" class="menu-redirect">Nos salles</a>
				</li>

				<li>
					<a href="{{ path("concept") }}" class="menu-redirect">Concept</a>
				</li>

				{# <li>
					<a href="{{ path("user_index") }}">EG virtuel</a>
				</li> #}

				{% if app.user %}
					<li>
						<a href="{{ path("profile_index") }}" class="menu-redirect">
							{{app.user.username}}</a>
					</li>

					<li>
						<a href="{{ path("app_logout") }}" class="menu-redirect">Déconnexion</a>
					</li>

				{% else %}
					<li>
						<a href="{{ path("app_login") }}" class="menu-redirect">Connexion</a>
					</li>

					<li>
						<a href="{{ path("app_register") }}" class="menu-redirect">Inscription</a>
					</li>
				{% endif %}
			</ul>
		</nav>
		{% if is_granted("ROLE_ADMIN") %}
		<hr>
			<nav>
				<ul class="menu">
					<li>
						<a href="{{ path('game_list')}}" class="menu-redirect menu-redirect-admin">Gestion des jeux</a>
					</li>
					<li>
						<a href="{{ path('calendar_index')}}" class="menu-redirect menu-redirect-admin">Gestion du calendrier</a>
					</li>
					<li>
						<a href="{{ path('user_index')}}" class="menu-redirect menu-redirect-admin">Gestion Utilisateurs / Clients</a>
					</li>
					<li>
						<a href="{{ path('order_index')}}" class="menu-redirect menu-redirect-admin">Gestion des réservations</a>
					</li>
				</ul>
			</nav>
		{% endif %}
	</div>
</header>



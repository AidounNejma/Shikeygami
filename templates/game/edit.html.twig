{% extends 'base.html.twig' %}

{% block stylesheets %}
	{{parent()}}
	{{ encore_entry_link_tags('game_add') }}
{% endblock %}

{% block javascripts %}
	{{parent()}}
	{{ encore_entry_script_tags('game_add') }}
{% endblock %}

{% block title %}Shikeygami | Edition de salle
{% endblock %}

{% block body %}
	<main>
		
		<div class="content">
		<section class="centered">
			<h1 id="title">Edition de salle</h1>
		</section>

			{{ include('game/_form.html.twig', {'button_label': 'Update'}) }}

			<a href="{{ path('game_index') }}">Retour à la liste</a>

			{{ include('game/_delete_form.html.twig') }}
		</div>
	</main>
{% endblock %}

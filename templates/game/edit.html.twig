{% extends 'base.html.twig' %}

{% block stylesheets %}
	{{parent()}}
	{{ encore_entry_link_tags('game') }}
{% endblock %}

{% block javascripts %}
	{{parent()}}
	{{ encore_entry_script_tags('game') }}
{% endblock %}

{% block title %}Edition de salle
{% endblock %}

{% block body %}
	<main>
		<section class="centered">
			<h1>Edition de salle</h1>
		</section>
		<div class="content">

			{{ include('game/_form.html.twig', {'button_label': 'Update'}) }}

			<a href="{{ path('game_index') }}">Retour à la liste</a>

			{{ include('game/_delete_form.html.twig') }}
		</div>
	</main>
{% endblock %}
